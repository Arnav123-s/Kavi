# Correction of the radius-bound problem

Author: [Arnav123-s](https://github.com/Arnav123-s)

This is a subsequent practice correction to Crowell and Shotwell, *Problems in Introductory Physics*, 7-m5. The original fresh-question score stays 4/5. There is no new unseen inequality examination in this correction.

Three intermediate lessons use this published problem's quantities: a height difference of 12 m from 35 m and 23 m; a remaining normal-force fraction of 1/3 from 2/3; and the upper bound of 72 m. Search finite zero-, one- and two-call arrangements of retained arithmetic, one, half and double. Record all search work and ambiguity. These are annotated worked quantities, not generated training questions. Save the resulting four definitions with dependency hashes.

The teaching interface supplies the physical assumptions and derivation. Starting from rest with strictly positive frictional loss gives $v^2<2g(h_0-h_1)$. At a circular crest, $mg-N=mv^2/r$ and $N=fmg$ give $v^2=(1-f)gr$. With $g>0$, $h_0>h_1\geq0$, and $0\leq f<1$, division by positive quantities gives:

$$0<r<\frac{2(h_0-h_1)}{1-f}.$$

The executable interface rejects missing premises and values outside that domain. It has not learned an arbitrary theorem prover or derived the premises from a picture. A matching numerical program selected from one corrected problem still needs broader evaluation. The proof rule is supplied mathematics, and the inference about a physical system is conditional on the stated assumptions.

Use the same finite visible worker and Pause/Resume/Stop controls. Recheck the four previously solved fresh textbook questions and thirteen older practice targets. Report these as retention, not new generalization. Allow up to five minutes, 512 MiB observed worker memory and five million counted units per fit.
